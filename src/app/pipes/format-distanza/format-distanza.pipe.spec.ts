import { FormatDistanzaPipe } from './format-distanza.pipe';

describe('FormatDistanzaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDistanzaPipe();
    expect(pipe).toBeTruthy();
  });
});
