import { FixencodedcharsPipe } from './fixencodedchars.pipe';

describe('FixencodedcharsPipe', () => {
  it('create an instance', () => {
    const pipe = new FixencodedcharsPipe();
    expect(pipe).toBeTruthy();
  });
});
